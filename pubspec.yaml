name: sg_cli
description: CLI tool for rapidly generating code for Flutter projects with BLoC.
version: 1.0.4
homepage: "https://github.com/solguruz/sg_cli"

environment:
  sdk: ^3.8.1

dependencies:
  args: ^2.7.0
  js: ^0.7.2
  package_config: ^2.2.0
  pub_semver: ^2.2.0
  web: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3

executables:
  sg:

# Flutter section may be required for proper dart pub global activate packaging
flutter: